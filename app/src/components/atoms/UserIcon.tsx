import { Box } from '@chakra-ui/react';
import React from 'react';

const UserIcon = () => {
  return (
    <Box
      border={'solid 1px'}
      borderRadius={'50%'}
      w={90}
      h={90}
      m={'0 auto'}
    ></Box>
  );
};

export default UserIcon;
